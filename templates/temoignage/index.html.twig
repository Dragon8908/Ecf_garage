{% extends 'base.html.twig' %}

{% block title %}Donnez votre avis{% endblock %}

{% block body %}
<div class="row">
    <div class="col"></div>
    <div class="col form-fonts">
        <h1 class="mb-3 text-center mt-3 text-primary">Avis</h1>
        {{ form_start(temoignageForm, {'attr': {'id': 'temoignage-form'}}) }}
        <div class="text-primary">Nom</div>
        {{ form_row(temoignageForm.nom) }}
        <div class="text-primary">Commentaire</div>
        {{ form_row(temoignageForm.commentaire) }}
        <div class="text-primary">Note</div>
        {{ form_row(temoignageForm.note) }}
        <button type="submit" class="mt-3 w-100 login-btn mb-2 text-secondary">Publier</button>
        {{ form_end(temoignageForm) }}
      
    </div>
    <div class="col"></div>
</div>  
<div class="raw mb-0">
    <div class="col text-primary text-center">
    Votre avis sera modéré par un employé avant d'être publié, merci de ne pas être menaçant ou injurieux, tout avis non argumenté ne sera pas publié
    Si tout va bien votre avis sera publié d'ici trois jours maximum sur la page d'accueil
    </div>
</div>
{% endblock %}